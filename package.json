{
  "name": "myWorkDirectory",
  "version": "0.0.1",
  "description": "the work directory for build mobile pages",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mayo",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-stylus": "^2.0.1",
    "gulp-uglify": "^1.2.0"
  }
}
